appId: com.chisa.terminal
productName: ChisaTerminal
directories:
  output: dist-build
files:
  - dist/**/*
  - hooks/**/*
  - package.json
  # Let electron-builder collect production dependencies (do not ship all of node_modules)
asar: true
asarUnpack:
  - hooks/**/*
  - node_modules/node-pty/**/*
win:
  target: nsis
  icon: build/icon.png
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
electronDownload:
  cache: "${env.ELECTRON_CACHE}"
